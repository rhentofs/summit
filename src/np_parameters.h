#ifndef NPPARAMETERS_H
#define NPPARAMETERS_H

#include <bitset>

using namespace std;

class NPParameters
{
	
public:
	NPParameters(int size);
	NPParameters(NPParameters const& par);
	~NPParameters();
	
};

#endif
